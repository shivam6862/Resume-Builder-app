import React from "react";
import styles from "../Styles/Header.module.css";

const Header: React.FC = () => {
  return <div className={styles.heading}>Resume Builder</div>;
};

export default Header;
