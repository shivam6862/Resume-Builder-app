module.exports = updateUser = async (userid, place, id, userUpdate) => {
  try {
    console.log(userid, place, id, userUpdate);
    const response = {};
    return response;
  } catch (err) {
    console.log(err.message);
    throw err;
  }
};
